sonar.projectKey=iot-fleet-ota-platform
sonar.projectName=IoT Fleet & OTA Management Platform

# Scoped to the ingestion path on purpose: this is where concurrency bugs
# and unhandled exceptions in long-running asyncio loops actually bite,
# not generic repo-wide style nits.
sonar.sources=backend/app/ingestion.py,backend/app/shadow.py,backend/app/rollout.py,backend/app/engine_main.py
sonar.tests=backend/tests
sonar.python.version=3.12
