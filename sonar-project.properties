sonar.projectKey=home-automation-platform
sonar.projectName=Home & Factory Automation Platform

# Scoped to the async control-plane modules on purpose: this is where
# concurrency bugs and unhandled exceptions in long-running asyncio loops
# actually bite when real appliances and sensors are on the other end, not
# generic repo-wide style nits.
sonar.sources=backend/app/ingestion.py,backend/app/shadow.py,backend/app/control.py,backend/app/usage_monitor.py,backend/app/factory.py,backend/app/household.py,backend/app/engine_main.py
sonar.tests=backend/tests
sonar.python.version=3.12
